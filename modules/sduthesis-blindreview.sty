%%
%% sduthesis-blindreview.sty — 盲审模式模块
%% 隐藏作者、学号、导师、答辩委员会成员等个人信息
%%
%% Copyright (C) 2025 h1s97x
%%
%% This work may be distributed and/or modified under the
%% conditions of the LaTeX Project Public License, either
%% version 1.3c of this license or (at your option) any later
%% version. This version of this license is in
%%    https://www.latex-project.org/lppl/lppl-1-3c.txt
%% and the latest version of this license is in
%%    https://www.latex-project.org/lppl.txt
%% and version 1.3 or later is part of all distributions of
%% LaTeX version 2020/11/27 or later.
%%
%% This work has the LPPL maintenance status "maintained".
%%
%% The Current Maintainer of this work is h1s97x.
%%
\ProvidesPackage{sduthesis-blindreview}[2026/08/18 v2.2.0 Blind review module for SDUThesis]

% ============================================================
% 盲审模式模块（叠加层）
% 通过 \SDUSetup{module={master,blindreview}} 等组合加载，
% 需与基础模块（undergraduate/master）一起使用。
%
% 工作原理：
%   1. 设置盲审标志 \l__sdu_blindreview_bool，基础模块封面据此
%      隐藏作者、学号、导师等个人信息行；
%   2. 覆盖个人信息 Getter，摘要/致谢等处的作者名同样隐藏；
%   3. 跳过声明页；
%   4. 跳过答辩委员会页（主席/委员姓名不进入盲审稿）。
%
% 说明：这里用 \renewcommand 而非 \RenewDocumentCommand 覆盖 Getter。
% 因为回归测试用 \ASSERT（内部 \edef）比较 Getter 展开值，
% 而 \RenewDocumentCommand 生成的 xparse 命令不可 \edef 展开
% （\edef 后仍是命令名而非其内容），导致断言恒失败。
% 改用 \renewcommand 后 \GetAuthor 退化为普通宏，可直接 \edef 为 ***。
% 该做法与 sduthesis 仓库完全一致。
% ============================================================

% ===== 向后兼容：单独使用 blindreview 时的本科回退已由内核加载器处理 =====
% 内核 \sdu_load_module: 检测到列表中含 blindreview 但无基础模块时，
% 会前置加载本科模块；此处不再重复加载，避免与书写顺序耦合。
% 参见 src/sduthesis.dtx 的模块加载器说明。

% ===== 设置盲审标志 =====
\ExplSyntaxOn
\bool_set_true:N \l__sdu_blindreview_bool
\ExplSyntaxOff

%% ===== 覆盖 Getter：隐藏个人信息 =====
%% 用 \renewcommand 覆盖（与 sduthesis 一致），
%% 使 Getter 退化为普通宏，可被回归测试的 \ASSERT(\edef) 展开比较。
\renewcommand{\GetAuthor}{***}
\renewcommand{\GetStudentId}{***}
\renewcommand{\GetSupervisor}{***}
\renewcommand{\GetCommitteeChair}{***}
\renewcommand{\GetCommitteeMembers}{***}

%% ===== 跳过声明页 =====
\renewcommand{\makestatement}{%
  % 盲审模式：跳过声明页
}

%% ===== 跳过答辩委员会页（盲审稿不输出主席/委员姓名） =====
\renewcommand{\makecommittee}{%
  % 盲审模式：跳过答辩委员会页，避免泄露主席/委员信息
}

\endinput
